{{#if quantity}}
<ul>
  {{#each cart_items}}
  <li>
    <figure>
      <img src="{{image}}" data-id="{{id}}">
    </figure>
    <figcaption>
      {{quantity}} x {{price}}
    </figcaption>
  </li>
  {{/each}}
</ul>
<div class="cart_summary">
  <h3>Your<br>Shopping Cart</h3>
  <p>${{format_price total}}</p>
  <a href="#" class="empty">Empty Cart</a>
  <a href="/checkout">Checkout</a>
</div>
{{/if}}